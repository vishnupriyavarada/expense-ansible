- name : Configure mysql
  hosts : mysql
  become : yes
  tasks :
    - name : task1 - Install mysql
      ansible.builtin.package :
        name : mysql-server
        state : installed

    - name : task1 - Start  and enable mysql service
      ansible.builtin.service :
        name : mysqld
        state : started
        enabled: yes
